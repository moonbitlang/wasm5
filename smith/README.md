Wasm fixtures used by parsing/validation tests.

Regenerate the smith fixtures:

  ./smith/gen-fixtures.sh

This uses wasm-tools smith with a conservative feature set that matches the
current parser/validator coverage.
